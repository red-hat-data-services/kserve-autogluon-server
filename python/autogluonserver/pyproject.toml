[project]
authors = [
    {name = "Michal Zabinski", email = "mzabinsk@redhat.com"},
    {name = "Dorota Laczak", email = "dlaczak@redhat.com"}
]
license = {text = "Apache-2.0"}
requires-python = "<3.13,>=3.10"
dependencies = [
    "kserve @ file:///${PROJECT_ROOT}/../kserve",
    "kserve-storage @ file:///${PROJECT_ROOT}/../storage",
    "autogluon.tabular[lightgbm,xgboost,tabm,fastai]==1.5.0+rhaiv.3",
    "autogluon.timeseries==1.5.0+rhaiv.3",
]
name = "autogluonserver"
version = "0.1.0"
description = "Model Server implementation for AutoGluon TabularPredictor and TimeSeriesPredictor. Not intended for use outside KServe Frameworks Images."
readme = "README.md"

[dependency-groups]
test = [
    "pytest<8.0.0,>=7.2.0",
    "pytest-cov<5.0.0,>=4.0.0",
    "mypy<1.0,>=0.991",
    "pytest-asyncio<1.0.0,>=0.23.4",
]
dev = [
    "black[colorama]~=24.3.0",
]

[tool.uv]
# Red Hat custom index as the primary source for all packages
index-url = "https://console.redhat.com/api/pypi/public-rhai/rhoai/3.5-EA2/cpu-ubi9/simple/"
extra-index-url = ["https://pypi.org/simple/"]
index-strategy = "unsafe-best-match"

[tool.uv.sources]
# Local path dependencies
kserve = { path = "../kserve", editable = false }
kserve-storage = { path = "../storage", editable = false }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
